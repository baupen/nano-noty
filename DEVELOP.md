# Develop

This is a maintained version of noty with minimal effort. Compatible with original with the preserved features. No refactoring has been done if not necessary (e.g., utils.js remains a mess).

## Publish

```
npm version patch
git push origin main --tags
```

or choose version `minor`, `major`
